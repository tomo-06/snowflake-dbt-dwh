version: 2

sources:
  - name: taxi_raw
    database: TAXI_DWH
    schema: RAW
    tables:
      - name: RAW_YELLOW_TRIPDATA
        description: "NYC Yellow Taxi trip records loaded via COPY INTO"
      - name: RAW_TAXI_ZONE_LOOKUP
        description: "Taxi zone lookup (LocationID → borough/zone)"
